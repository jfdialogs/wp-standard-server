#!/usr/bin/env bash

if [ -n "$PROJECT_DIR" ] && [ -n "$DOCKER_DIR" ]; then
  echo "Running shell within data container..."
  echo ""

  ENV_FILE="$PROJECT_DIR/.env"

  if [ -f "$ENV_FILE" ]; then
    docker-compose -f "$PROJECT_DIR/compose-local.yaml" --env-file "$ENV_FILE" exec data bash
  else
    echo "Environment file $ENV_FILE missing. Aborting."
  fi
else
  echo "PROJECT_DIR and DOCKER_DIR are required."
fi
